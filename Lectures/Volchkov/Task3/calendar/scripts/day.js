class Day {

    constructor(dayOfMonth, isActiveDay, isCurrentDay, isWeekend) {

        this.dayOfMonth = dayOfMonth;
        this.isActiveDay = isActiveDay;
        this.isCurrentDay = isCurrentDay;
        this.isWeekend = isWeekend;
    }
}